# @vizuh/clicktrail-wordpress

WordPress and WooCommerce form/order metadata helpers. Use server-side hooks and consent checks before persisting the returned allowlisted fields. Browser values never establish tenant or identity authority.

# @vizuh/clicktrail-node

Node.js helpers for ClickTrail's attribution handoff layer: extract allowlisted
click IDs from requests, attach them to server-owned conversion records, and
generate idempotency keys for webhook delivery.

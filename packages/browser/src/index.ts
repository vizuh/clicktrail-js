/**
 * @vizuh/clicktrail-browser — browser SDK.
 *
 * Consent-aware capture, first-party storage, forms, dataLayer/HTTP
 * destinations, SPA page views. Effects only run post-start().
 */
export * from './browser/index.js';

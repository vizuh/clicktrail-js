# @vizuh/clicktrail-shopify

Shopify Web Pixel and order webhook helpers. Capture click IDs in a first-party boundary, carry them through checkout note attributes, and use the Shopify order ID as the server-side idempotency source. Browser data and note attributes are not trusted identity.

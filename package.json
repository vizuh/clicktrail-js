{
  "name": "clicktrail-js",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:global": "pnpm --filter @vizuh/clicktrail build:global",
    "test": "pnpm -r test",
    "probe": "pnpm --filter @vizuh/clicktrail build && pnpm --filter @vizuh/clicktrail build:global && node probe/run-probe.mjs",
    "parity": "node tools/wp-runtime/run-parity.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.6.0"
  }
}
